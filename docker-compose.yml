services:
  web:
    build: .
    image: rodamientin-web:latest
    container_name: rodamientin-web
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
